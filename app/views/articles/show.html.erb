<div class="mx-5">
<div class="row mt-5 pb-3 mb-3 border-bottom border-gray">

  <div class="col-12 col-md-8">
    <h2 class="text-center fw-bold fs-1"><%= @article.title %></h2>
    <p class="text-center text-s mt-3">Published in <%= @article.created_at.strftime("%b %d, %Y") %></p>
    <h3 class="fs-6 text-center mt-4">Lorem ipsum</h3>

    <div class="text-center mt-4 mb-3">
      <%= link_to "Edit", edit_article_path(@article), class: "p-1 px-2 bg-mandarin-sorbet rounded text-light reset-a me-2"%>
      <%= link_to "Delete", article_path(@article), class: "p-1 px-2 bg-light-red rounded text-light reset-a",
      data: {turbo_method: :delete, turbo_confirm: "Delete #{@article.title}?"} %>
    </div>
  </div>

  <div class="col-12 col-md-4">
      <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/skateboard.jpg" class="img-fluid rounded-1"/>
  </div>

</div>

<div class="row">
  <div class="col-12">
    <p class="text-gray fs-5"><%= @article.content %></p>
  </div>
</div>

</div>
